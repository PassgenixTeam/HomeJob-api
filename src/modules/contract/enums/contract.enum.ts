export enum CONTRACT_STATUS {
  PENDING = 'pending',
  STARTED = 'started',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
}

export enum PAY_STATUS {
  PENDING = 'pending',
  PAID = 'paid',
}

export enum PAY_TYPE {
  HOURLY = 'hourly',
  FIXED = 'fixed',
}

export enum OFFER_STATUS {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
